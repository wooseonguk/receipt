<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gourmet.DetailActivity" ></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCtDbHj5kiEaoZFKa0jpi62j9D6E-i2ML4" />

        <activity android:name=".search.SearchActivity" android:noHistory="true" />
        <activity android:name=".month.MonthActivity" android:noHistory="true" />
        <activity android:name=".gourmet.GourmetActivity" android:noHistory="true" />
        <activity android:name=".local.LocalActivity" android:noHistory="true" />
        <activity android:name=".MainActivity" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>